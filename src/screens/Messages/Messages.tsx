import { AppLayout } from "@muc/layout";
import { MessageContainer } from "@muc/modules";

const Messages = () => {
  return(

      <AppLayout>

  <MessageContainer />
  </AppLayout>
)
  ;
};

export default Messages;
